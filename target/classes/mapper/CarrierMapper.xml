<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ln.tms.mapper.CarrierMapper">

    <!-- 查询承运商信息 -->
    <select id="queryAllCarrier" resultType="Carrier">
        SELECT
        carrier_code,
        carrier_name,
        FROM
        tms_carrier
    </select>
</mapper>